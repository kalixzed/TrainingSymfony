<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Administration{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22>
            <text y=%221.2em%22 font-size=%2296%22>⚫️</text>
            <text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" 
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>

        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
                <!-- Lien vers une page d'acceuil en utilisant la fonction url -->
                <a class="navbar-brand" href="{{ url('admin.recipe.index') }}">Administration</a>
                <div class="collapse navbar-collapse">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <!-- Ajout d'une fonction "app.current_route starts with pour effectuer des vérifications
                            et emmettre des conditions si la véréfication s'avère vrai ou fausse  -->
                            <!-- Ainsi donc la route sera active lorsque l'URL commencera par 'admin.recipe.' -->
                            <a class="nav-link {{ app.current_route starts with 'admin.recipe.' ? 'active': '' }}" href="{{ path('admin.recipe.index') }}">Recettes</a>
                        </li>
                        <li class="nav-item">
                            <!-- Ajout d'une fonction "app.current_route starts with pour effectuer des vérifications
                            et emmettre des conditions si la véréfication s'avère vrai ou fausse  -->
                            <!-- Ainsi donc la route sera active lorsque l'URL commencera par 'admin.category.' -->
                            <a class="nav-link {{ app.current_route starts with 'admin.category.' ? 'active': '' }}" href="{{ path('admin.category.index') }}">Catégories</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        <div class="container my-4">
            {% include 'partials/flash.html.twig' %}
            <!-- Affichage des messages flash pour les notifications utilisateurs -->
            {% block body %}
            {% endblock %}
        </div>
    </body>
</html>

